{
  "name": "entities",
  "version": "1.0.0",
  "description": "",
  "main": "entities.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "xslt": "npx xslt3 -xsl:tei-to-json.xsl -export:tei-to-json.sef.json -t -nogo",
    "build": "node entities.js",
    "watch": "watch 'npm run build' ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "watch": "^1.0.2"
  },
  "dependencies": {
    "saxon-js": "^2.5.0"
  }
}
