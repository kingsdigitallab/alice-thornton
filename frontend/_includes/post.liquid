---
layout: base.liquid
eleventyComputed:
  pageImageWebPath: "{{ feature }}"
  buggy11ty: "{{ postCategory }}"
  description: "{{ postCategory.label }} article - {{ date | date:metadata.date_format }}"
---

{% include "post_meta.liquid", post: context %}

{{ content }}
