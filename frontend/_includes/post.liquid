---
layout: base.liquid
eleventyComputed:
  pageImageWebPath: "{{ feature }}"
  description: "{{ categories }} article - {{ date | date:metadata.date_format }}"
---

{% include "post_meta.liquid", post: context %}

{{ content }}
